name: text_import_excel
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: 3.5.0

dependencies:
  flutter:
    sdk: flutter
  excel: 4.0.6
  file_picker: 8.1.2
  cloud_firestore: 4.14.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 4.0.0

flutter:
  uses-material-design: true
